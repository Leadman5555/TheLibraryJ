import {BlockRouterLinkDirective} from './block-router-link.directive';

describe('BlockRouterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockRouterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
